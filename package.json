{
  "name": "seenoise",
  "version": "0.1.0",
  "private": true,
  "description": "Noise-pollution exposure prediction for Klang Valley high-rises — Cloudflare Worker + D1 backend and React frontend.",
  "scripts": {
    "postinstall": "cd web && npm install",
    "build": "cd web && npm run build",
    "deploy": "npm run build && npx wrangler deploy",
    "dev:worker": "npx wrangler dev",
    "dev:web": "cd web && npm start",
    "d1:console": "npx wrangler d1 execute seenoise --remote"
  },
  "devDependencies": {
    "wrangler": "^4.129.0"
  }
}
